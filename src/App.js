import React, {useState} from "react";
import styled, {createGlobalSyle, css} from "styled-components";

const GlobalSttyle = createGlobalSyle`
`

const sharedStyles = css `
`


function App() {
  return <div className="App">app</div>;
}

export default App;
